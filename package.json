{
    "name": "slipped-stitch",
    "version": "1.0.0",
    "description": "",
    "main": ".eleventy.js",
    "scripts": {
        "eleventy:serve": "eleventy --serve",
        "eleventy:build": "eleventy",
        "sass:watch": "sass src/sass:dist/css --watch --update --color",
        "sass:build": "sass src/sass:dist/css --no-source-map --style=compressed",
        "clean": "rm -rf dist/",
        "start": "run-p -l clean sass:build sass:watch eleventy:serve",
        "build": "run-s -l eleventy:build sass:build"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "dependencies": {
        "@11ty/eleventy": "^1.0.0",
        "path": "^0.12.7",
        "sass": "^1.49.8"
    },
    "devDependencies": {
        "@11ty/eleventy-img": "^1.0.0",
        "npm-run-all": "^4.1.5"
    }
}
